window.FilterEngine={apply(items,state){return items.filter(p=>(!state.cat||p.category===state.cat)&&(!state.brand||p.brand===state.brand)&&p.price>=state.min&&p.price<=state.max&&p.rating>=state.rating)} ,sort(items,key){return [...items].sort((a,b)=>key==='low'?a.price-b.price:key==='high'?b.price-a.price:key==='rating'?b.rating-a.rating:key==='new'?(b.new?1:0)-(a.new?1:0):0)}};
