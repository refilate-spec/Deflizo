window.Favorites={key:'nova_saved',get(){return JSON.parse(localStorage.getItem(this.key)||'[]')},has(id){return this.get().includes(Number(id))},toggle(id){id=Number(id);let a=this.get(),i=a.indexOf(id);i<0?a.push(id):a.splice(i,1);localStorage.setItem(this.key,JSON.stringify(a));this.refresh();return i<0},refresh(){document.querySelectorAll('[data-action="save"]').forEach(b=>{let on=this.has(b.dataset.id);b.classList.toggle('text-rose-500',on);b.innerHTML=`<i class="fa-${on?'solid':'regular'} fa-heart"></i>`});document.querySelectorAll('[data-saved-count]').forEach(x=>x.textContent=this.get().length)}};
