(function(){function enhance(root){const sc=root.querySelector('[data-slider]');if(!sc)return;root.querySelector('[data-slide="prev"]')?.addEventListener('click',()=>sc.scrollBy({left:-sc.clientWidth*.78,behavior:'smooth'}));root.querySelector('[data-slide="next"]')?.addEventListener('click',()=>sc.scrollBy({left:sc.clientWidth*.78,behavior:'smooth'}));let drag=false,sx=0,sl=0;sc.addEventListener('pointerdown',e=>{drag=true;sx=e.clientX;sl=sc.scrollLeft;sc.setPointerCapture(e.pointerId);sc.classList.remove('snap-x')});sc.addEventListener('pointermove',e=>{if(drag)sc.scrollLeft=sl-(e.clientX-sx)*1.15});['pointerup','pointercancel'].forEach(x=>sc.addEventListener(x,()=>{drag=false;sc.classList.add('snap-x')}));}window.Slider={enhance};})();
